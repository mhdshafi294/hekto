import {
  blogsDetailed,
  blogsRecent,
  blogsSale,
  blogProducts,
} from "@/constants/constants";

const Blog = (props: any) => {
  const { params, searchParams } = props;

  return (
    <div>
      <div></div>
    </div>
  );
};

export default Blog;
